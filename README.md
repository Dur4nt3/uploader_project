# File Uploader

## File Uploader - Overview

**Important Note: This project is solely using TypeScript because `prisma-client-js` is deprecated, thus requiring me to use `prisma-client` which requires TypeScript.**
**Therefore, in various parts of the project I've opted to use the simplest types to allow the application to compile.**
